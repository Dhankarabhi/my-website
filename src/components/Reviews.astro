---
import svgNew from '../assets/new.svg'
import svgPinkStar from '../assets/pink-star.svg'
import svgRedStar from '../assets/red-star.svg'

const achievements = [
  { 
    title: "Cloud Infrastructure Optimization",
    description: "Reduced AWS costs by 40% while improving system performance through intelligent resource allocation and automated scaling policies",
    metrics: "40% Cost Reduction",
    impact: "Saved $50K+ annually",
    technologies: ["AWS", "Terraform", "CloudWatch"],
    icon: "☁️",
    category: "Cost Optimization",
    timeline: "6 months",
    cls: `hover:rotate-1`
  },
  { 
    title: "CI/CD Pipeline Implementation", 
    description: "Automated deployment process reducing release time from days to minutes with zero-downtime deployments",
    metrics: "95% Faster Deployments",
    impact: "From 3 days to 30 minutes",
    technologies: ["Jenkins", "Docker", "Kubernetes"],
    icon: "🚀",
    category: "Automation",
    timeline: "4 months",
    cls: `hover:-rotate-1`
  },
  { 
    title: "Zero-Downtime Architecture",
    description: "Achieved 99.9% uptime with automated failover, load balancing, and comprehensive disaster recovery",
    metrics: "99.9% Uptime",
    impact: "Zero production incidents",
    technologies: ["Kubernetes", "Istio", "Prometheus"],
    icon: "⚡",
    category: "Reliability",
    timeline: "8 months",
    cls: `hover:rotate-1`
  },
  { 
    title: "Security & Compliance Framework",
    description: "Implemented security scanning and compliance monitoring achieving SOC 2 certification",
    metrics: "100% Security Coverage",
    impact: "SOC 2 compliance achieved",
    technologies: ["Trivy", "Falco", "OPA"],
    icon: "🔐",
    category: "Security",
    timeline: "5 months",
    cls: `hover:-rotate-1`
  },
  { 
    title: "Monitoring & Observability Platform",
    description: "Built comprehensive observability reducing Mean Time To Resolution by 80% with proactive alerting",
    metrics: "80% Faster Resolution",
    impact: "5 minutes avg MTTR",
    technologies: ["Prometheus", "Grafana", "ELK"],
    icon: "📊",
    category: "Monitoring",
    timeline: "3 months",
    cls: `hover:rotate-1`
  },
  { 
    title: "Multi-Cloud Strategy",
    description: "Designed hybrid cloud architecture across AWS, Azure, and GCP with seamless workload distribution",
    metrics: "3 Cloud Providers",
    impact: "99.95% availability",
    technologies: ["AWS", "Azure", "GCP", "Terraform"],
    icon: "🌐",
    category: "Architecture",
    timeline: "12 months",
    cls: `hover:-rotate-1`
  }
]

const stats = [
  { number: "15+", label: "Production Deployments", icon: "🚢" },
  { number: "99.9%", label: "System Uptime", icon: "⚡" },
  { number: "50+", label: "Automated Pipelines", icon: "🔄" },
  { number: "$200K+", label: "Cost Savings", icon: "💰" }
]
---

<section class="py-12 px-5 relative overflow-hidden">
  <img src={svgPinkStar.src} alt="svg" width="60" class="absolute -right-6 top-10 opacity-20 animate-pulse" />
  <img src={svgRedStar.src} alt="svg" width="40" class="absolute left-10 bottom-20 opacity-20 animate-bounce" />
  
  <div class="max-w-6xl mx-auto">
    
    <!-- Section Header -->
    <div class="text-center mb-12 relative">
      <div class="inline-block relative">
        <h1 class="text-4xl md:text-5xl font-bold mb-2">
          Proven Track Record of 
          <span class="relative inline-block">
            <span class="absolute bg-[var(--yellow)] w-full h-[50%] left-0 bottom-0 z-1 animate-pulse"></span>
            <span class="relative z-10">Success</span>
          </span>
        </h1>
        <div class="absolute -top-2 -right-8 transform rotate-12 animate-bounce">
          <img src={svgNew.src} alt="highlight" width="40" />
        </div>
      </div>
      <p class="text-lg text-gray-600 mt-4 max-w-3xl mx-auto">
        Real results that drive business value and operational excellence in production environments
      </p>
    </div>

    <!-- Key Statistics with Animation -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6 mb-12">
      {stats.map((stat, index) => (
        <div class={`bg-white border-3 border-black p-4 md:p-6 text-center shadow-[4px_4px_0px_rgba(0,0,0,0.3)] hover:shadow-[8px_8px_0px_rgba(0,0,0,0.3)] hover:-translate-y-2 transition-all duration-300 group animate-fade-in-up`} style={`animation-delay: ${index * 150}ms`}>
          <div class="text-2xl md:text-3xl mb-2 group-hover:scale-110 transition-transform duration-300">{stat.icon}</div>
          <div class="text-xl md:text-3xl font-bold text-[var(--blue)] mb-1 group-hover:text-black transition-colors duration-300">{stat.number}</div>
          <div class="text-xs md:text-sm font-medium text-gray-700">{stat.label}</div>
        </div>
      ))}
    </div>

    <!-- Main Header with Better Positioning -->
    <div class="flex flex-col md:flex-row md:items-end md:justify-between mb-10">
      <div class="relative mb-4 md:mb-0">
        <h2 class="text-2xl md:text-3xl font-bold max-w-md leading-tight">
          Here's what I've accomplished that matters most
        </h2>
        <div class="absolute -bottom-1 left-0 w-20 h-1 bg-[var(--yellow)] animate-pulse"></div>
      </div>
      <div class="relative">
        <p class="text-base md:text-lg text-gray-600 max-w-md md:text-right">
          Each achievement represents measurable business impact and technical excellence
        </p>
        <img src={svgNew.src} alt="arrow" width="35" class="absolute -right-8 -top-2 animate-bounce hidden md:block" />
      </div>
    </div>

    <!-- Achievements Grid with Staggered Animation -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
      {achievements.map((item, index) => ( 
        <div class={`bg-white border-3 border-black shadow-[6px_6px_0px_rgba(0,0,0,0.3)] hover:shadow-[12px_12px_0px_rgba(0,0,0,0.3)] transition-all duration-500 hover:-translate-y-3 group animate-slide-in ${item.cls}`} style={`animation-delay: ${index * 200}ms`}>
          
          <!-- Card Header with Gradient -->
          <div class="bg-gradient-to-r from-[var(--yellow)] to-[var(--blue)] border-b-3 border-black p-4 relative overflow-hidden">
            <div class="flex items-center justify-between mb-2">
              <span class="text-2xl group-hover:animate-bounce">{item.icon}</span>
              <span class="px-3 py-1 bg-black text-white text-xs font-bold uppercase tracking-wider">
                {item.category}
              </span>
            </div>
            <h3 class="text-base md:text-lg font-bold text-black leading-tight group-hover:text-gray-800 transition-colors duration-300">{item.title}</h3>
            <div class="absolute -right-6 -bottom-6 w-16 h-16 bg-white opacity-20 rounded-full group-hover:scale-150 transition-transform duration-500"></div>
          </div>

          <!-- Card Content -->
          <div class="p-4 md:p-5 space-y-4">
            <p class="text-gray-700 text-sm leading-relaxed group-hover:text-gray-900 transition-colors duration-300">{item.description}</p>
            
            <!-- Impact Metrics with Animation -->
            <div class="bg-gray-50 border-2 border-gray-200 p-3 group-hover:bg-[var(--yellow)] group-hover:border-black transition-all duration-300">
              <div class="text-xl md:text-2xl font-bold text-[var(--blue)] mb-1 group-hover:text-black transition-colors duration-300">{item.metrics}</div>
              <div class="text-sm text-gray-600 font-medium group-hover:text-gray-800 transition-colors duration-300">{item.impact}</div>
            </div>

            <!-- Technologies -->
            <div class="space-y-2">
              <div class="text-xs font-bold text-gray-500 uppercase tracking-wider">Tech Stack</div>
              <div class="flex flex-wrap gap-1">
                {item.technologies.map((tech, techIndex) => (
                  <span class={`px-2 py-1 bg-[var(--blue)] border border-black text-xs font-medium group-hover:bg-black group-hover:text-white transition-all duration-300`} style={`transition-delay: ${techIndex * 100}ms`}>
                    {tech}
                  </span>
                ))}
              </div>
            </div>

            <!-- Timeline with Better Styling -->
            <div class="flex items-center justify-between text-xs text-gray-500 pt-3 border-t border-gray-200">
              <span class="flex items-center gap-1">
                <span class="animate-pulse">⏱️</span> 
                <span class="font-medium">{item.timeline}</span>
              </span>
              <span class="text-green-600 font-bold flex items-center gap-1">
                <span class="animate-pulse">✅</span> 
                <span>Delivered</span>
              </span>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Enhanced Call-to-Action -->
    <div class="bg-gradient-to-r from-black to-gray-800 text-white border-3 border-black p-6 md:p-10 text-center relative overflow-hidden">
      <div class="relative z-10">
        <h3 class="text-2xl md:text-3xl font-bold mb-4 animate-fade-in">Ready to Deliver Similar Results?</h3>
        <p class="text-lg md:text-xl mb-6 opacity-90 max-w-2xl mx-auto animate-fade-in-up" style="animation-delay: 200ms">
          Let's discuss how I can bring this level of excellence to your team and infrastructure.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center animate-fade-in-up" style="animation-delay: 400ms">
          <a href="mailto:abhishekchoudhary2440@gmail.com" class="px-6 md:px-8 py-3 md:py-4 bg-[var(--yellow)] text-black font-bold border-3 border-black hover:bg-white hover:scale-105 transition-all duration-300 flex items-center justify-center gap-2">
            <span>📧</span>
            <span>Let's Connect</span>
          </a>
          <a href="/resume.pdf" target="_blank" class="px-6 md:px-8 py-3 md:py-4 bg-white text-black font-bold border-3 border-black hover:bg-[var(--blue)] hover:scale-105 transition-all duration-300 flex items-center justify-center gap-2">
            <span>📄</span>
            <span>View Resume</span>
          </a>
        </div>
      </div>
      
      <!-- Animated Background Elements -->
      <div class="absolute -right-10 -top-10 w-32 h-32 bg-[var(--yellow)] opacity-10 rounded-full animate-pulse"></div>
      <div class="absolute -left-10 -bottom-10 w-24 h-24 bg-[var(--blue)] opacity-10 rounded-full animate-bounce"></div>
      <img src={svgPinkStar.src} alt="star" width="50" class="absolute top-4 right-4 opacity-30 animate-spin-slow" />
    </div>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slide-in {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-slide-in {
    animation: slide-in 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-fade-in {
    animation: fade-in 1s ease-out forwards;
    opacity: 0;
  }

  .animate-spin-slow {
    animation: spin-slow 10s linear infinite;
  }

  /* Hover effects for cards */
  .group:hover .animate-bounce {
    animation-duration: 0.5s;
  }

  /* Responsive improvements */
  @media (max-width: 768px) {
    .animate-fade-in-up,
    .animate-slide-in,
    .animate-fade-in {
      animation-delay: 0ms !important;
    }
  }
</style>